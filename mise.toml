[tools]
node = "26.1.0"
bats = "1.13.0"
shellcheck = "0.10.0"
jq = "1.7.1"

[tasks.test]
description = "Run bats test suite"
run = "bats test/bats"

[tasks."test:shellcheck"]
description = "Run shellcheck on shell scripts"
run = "git ls-files -z '*.sh' | xargs -0 shellcheck -S error -x"
